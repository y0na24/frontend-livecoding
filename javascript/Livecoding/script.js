function findLargetsDigits(arr, k) {
	return arr.sort((a, b) => a - b).slice(-k)
}

console.log(sortTwoArrays(arr1, 3))

const sum = (a, b) => {
	return a + b
}

console.log(sum(2, 2))

const promise = new Promise((res, rej) => {
	res(5)
})

const getString = (v]) =>  {
	
}
